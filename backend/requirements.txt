fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
pydantic==2.10.4
pydantic-settings==2.7.1
alembic==1.14.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 não lê a versão do bcrypt >= 4.1 e emite erro no log; 4.0.1 evita isso
bcrypt==4.0.1
pyjwt==2.10.1
email-validator==2.2.0
# usado pelo worker de coleta (cliente da YouTube Data API)
httpx==0.28.1

# Pre-processamento COMPARTILHADO com o ml/ (pacote proprio na raiz do repo).
# O worker de inferencia precisa aplicar ao comentario que chega EXATAMENTE a mesma
# transformacao que o corpus de treino sofreu; por isso o codigo e um so, e nao uma
# copia de cada lado (CLAUDE.md Secao 3 proibe ml/ e backend/ se importarem).
# Sem dependencias proprias: e stdlib pura, nao pesa no ambiente de producao.
-e ./preprocessamento

# Classificador lexico COMPARTILHADO com o ml/ (pacote proprio na raiz do repo).
# E a primeira implementacao do worker de inferencia, enquanto o BERTimbau oficial
# nao existe -- e a MESMA regra que o ml/ usa como piso do Capitulo 5. Duas copias
# divergiriam e o numero do capitulo deixaria de descrever a producao.
# O ARQUIVO do SentiLex nao vem no pacote (dado de terceiro, 6,9 MB, fora do git):
# o caminho e SENTILEX_PATH e o sha256 esperado esta no codigo do classificador.
# Tambem stdlib pura.
-e ./lexico
