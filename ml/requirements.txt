# Dependências do pipeline de IA (ml/).
#
# Deliberadamente separado de backend/requirements.txt: o ml/ não importa nada do
# backend (CLAUDE.md Seção 3) e o backend de produção não carrega transformers
# para rodar a API.

# Acesso ao Postgres para ler COMENTARIOS e popular EXEMPLOS_TREINAMENTO.
asyncpg==0.30.0

# Tokenizer do BERTimbau. `tokenizers` é o backend rápido usado pelo AutoTokenizer;
# torch NÃO entra aqui — medir comprimento não exige carregar os pesos do modelo.
transformers==4.46.3

# Planilhas dos avaliadores humanos. xlsx (e nao CSV) para poder embutir validacao
# por lista nas tres classes: avaliador que digita "Positivo" ou "pos" quebraria o
# calculo do Kappa em silencio.
openpyxl==3.1.5

# Pre-processamento COMPARTILHADO com o backend (raiz do repo, pacote proprio).
# Instalado em modo editavel para que uma mudanca no mapa de emoji valha nos dois
# ambientes sem reinstalar:
#   ml/.venv/Scripts/python.exe -m pip install -e ./preprocessamento
-e ./preprocessamento

# Classificador lexico, tambem compartilhado. Nasceu aqui (ml/lexico/sentilex.py) e
# mudou de casa quando o worker de inferencia passou a precisar dele: o backend nao
# pode importar de ml/ (CLAUDE.md Secao 3) e a regra tem que ser uma so, senao o
# piso do Capitulo 5 e a classificacao de producao divergem em silencio.
# O ml/lexico/ continua com o que e experimento (classificar_teste.py, metadados).
-e ./lexico

# Figuras do Capitulo 5 (ml/avaliacao/graficos.py). Entra no ml/ e so nele: o
# backend de producao nao desenha grafico -- o dashboard e do Angular.
matplotlib==3.9.4
